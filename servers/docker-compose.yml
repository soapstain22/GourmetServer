version: "3.8"

services:
  mc:
    image: itzg/minecraft-server
    environment:
      EULA: "true"
      TYPE: vanilla
      # TYPE: SPIGOT
      # BUILD_FROM_SOURCE: true
      # 28140: luckperms
      # 34315: vault
      # 2124: Skinsrestorer
      # 88135: plugmanx
      SPIGET_RESOURCES: 28140,34315,88135
      SERVER_NAME: "Gourmet Server 1.20.2"
      MOTD: "§c§lG§6§lou§e§lr§2§lm§a§le§3§lt§b§l S§9§le§1§lr§5§lv§d§le§r§lr§r\ndiscord.gg/6BM2dn2sja"
      MEMORY: "4G"
      RESOURCE_PACK: "https://github.com/soapstain22/GourmetResource/raw/main/GourmetResource.zip"
      ONLINE_MODE: false
      ENFORCE_SECURE_PROFILE: FALSE
    volumes:
      - ./mc-data:/data
      - ./mc-plugins:/plugins
    stdin_open: true
    tty: true
    restart: unless-stopped
    ports:
      - "25565:25565"
volumes:
  mc:
    external: true